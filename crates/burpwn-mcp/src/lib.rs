//! burpwn-mcp — an MCP server (rmcp) exposing burpwn's capabilities as tools that map 1:1 to the
//! CLI (session/exec/query/replay/workspace/intercept), including the long-poll `await_intercept`
//! tool that surfaces blocking interception to an agent over stateless MCP tool calls.

// Implementation lands in M7.
