//! burpwn-cli — the clap subcommand tree (doctor, init, ca, session, exec, req, intercept,
//! match-replace, workspace/group/tag/note, export) with a uniform JSON envelope output and the
//! orchestration glue that wires the store, proxy, sandbox and TLS layers together.

// Implementation lands in M5.
